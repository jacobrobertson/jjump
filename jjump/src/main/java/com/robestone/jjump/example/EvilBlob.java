package com.robestone.jjump.example;


public class EvilBlob {
	/*
	extends PolygonSprite {
}
	
	private Random random = new Random();
	
	public EvilBlob() {
		super("Evil Blob");
		addPoint(0, 0);
		addPoint(50, 50);
		addPoint(50, 100);
		addPoint(75, 150);
		addPoint(50, 150);
		addPoint(0, 50);
		setPosition(100, 100);
		setEdgeBehavior(EdgeBehavior.Bounce);
		setMoving(1, 1);
	}
	public void foreverMorph() {
		int position = random.nextInt(getXPoints().size());
		boolean isX = random.nextBoolean();
		boolean isNegative = random.nextBoolean();
		
		int distance;
		if (isNegative) {
			distance = 1;
		} else {
			distance = -1;
		}
		List<Integer> points;
		if (isX) {
			points = getXPoints();
		} else {
			points = getYPoints();
		}
		int value = points.get(position);
		points.set(position, value + distance);
	}
	public void ifTyped_Space() {
		addPoint(9, 9);
	}
*/
}
